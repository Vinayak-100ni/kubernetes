#### check logs 
 kubectl logs nginx
#### check labels
 kubectl get pods  --show-labels
#### add labels
 kubectl label pod -l run=nginx work=tut
#### edit yml of pod , deployment ....
 kubectl edit pod nginx
              |      |
          (kind) (meta dataname, pod name)

#### how to check how many rollout will be happend in previous state
 kubectl rollout history deploy rollout-demo
                                    |
                              (deployment name)
